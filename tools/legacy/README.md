# Legacy migration scripts

These one-time generators were preserved from the imported archive for recovery history. They are **not** loaded by PermBot and can overwrite active modules if run from the repository root.

Use the current files in `cogs/` and `utils/` as the source of truth. Review a legacy script carefully before running it.
